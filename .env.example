# SMTP provider settings used by /api/contact
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
# Optional. Defaults to SMTP_USER.
SMTP_FROM=your-verified-sender@example.com
